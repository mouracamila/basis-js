//Operador COndicional (Ternário)

//Dependendo da condição, nés recebemos valores diferentes

//Condiçcão então valor 1 se não valor 2

//condition ? value1 : value2

//exemplos

//café da manhã
/* 
let pao = true
let queijo = false

const niceBreakfast = pao && queijo ? 'café nice' : 'café bad'

console.log(niceBreakfast);
*/

// maior de 18

let age = 16
const canDrive = age >= 18 ? 'can drive' : 'cannot drive'

console.log(canDrive);