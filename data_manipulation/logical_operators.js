// Operadores lógicos 

// -2 valores booleanos, quando verificados, resultartá em verdadeiro ou falso

let pao = true
let queijo = true

// AND &&
console.log(pao && queijo)

let banana = false
let uva = true 

console.log(banana && uva)

// OR || (pode retornar true apenas um elemento, se apenas uma variavel for verdadedeiro ele volta como verdadeiro)

console.log(banana || uva)


// NOT ! (muda o bolean da variavel)

console.log(!uva)